<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chi tiết người dùng</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card {
            border: none;
            border-radius: 10px;
        }
        .table th {
            background-color: #e9ecef;
            width: 30%;
        }
        .btn-secondary {
            background-color: #6c757d;
            border: none;
        }
        .btn-secondary:hover {
            background-color: #5a6268;
        }
        /* Định nghĩa màu sắc cho vai trò */
        .role-admin {
            background-color: yellow;
            color: black;
        }
        .role-user {
            background-color: #007bff; /* Màu xanh dương */
            color: white;
        }
        /* Định nghĩa màu sắc cho trạng thái */
        .status-active {
            background-color: #28a745; /* Màu xanh lá */
            color: white;
        }
        .status-inactive {
            background-color: #ffca28; /* Màu cam */
            color: black;
        }
        .status-locked {
            background-color: #dc3545; /* Màu đỏ */
            color: white;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h2 class="mb-4 text-center">Thông tin Chi tiết người dùng</h2>
        <div class="card shadow-sm">
            <div class="card-body">
                <table class="table table-striped">
                    <tbody>
                        <tr>
                            <th>Tên tài khoản</th>
                            <td><%= user.username %></td>
                        </tr>
                        <tr>
                            <th>Họ và tên</th>
                            <td><%= user.fullname %></td>
                        </tr>
                        <tr>
                            <th>Địa chỉ</th>
                            <td><%= user.address %></td>
                        </tr>
                        <tr>
                            <th>Giới tính</th>
                            <td>
                                <%= user.sex === 'male' ? 'Nam' : user.sex === 'female' ? 'Nữ' : 'Khác' %>
                            </td>
                        </tr>
                        <tr>
                            <th>Email</th>
                            <td><%= user.email %></td>
                        </tr>
                        <tr>
                            <th>Vai trò</th>
                            <td>
                                <span class="badge <%= user.role === 'admin' ? 'role-admin' : 'role-user' %>">
                                    <%= user.role %>
                                </span>
                            </td>
                        </tr>
                        <tr>
                            <th>Trạng thái tài khoản</th>
                            <td>
                                <span class="badge 
                                    <%= user.account_status === 'hoạt động' ? 'status-active' : 
                                       user.account_status === 'tạm ngưng' ? 'status-inactive' : 
                                       'status-locked' %>">
                                    <%= user.account_status %>
                                </span>
                            </td>
                        </tr>
                        <tr>
                            <th>Ngày tạo</th>
                            <td><%= user.created_at %></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="mt-4 text-center">
            <a href="/listuser" class="btn btn-secondary"><i class="fas fa-arrow-left"></i> Quay lại</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
